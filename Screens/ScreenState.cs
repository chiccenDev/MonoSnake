namespace MonoSnake.Screens
{
    /// <summary>
    /// Enum described the screen transition state.
    /// </summary>
    public enum ScreenState
    {
        TransitionOn,
        Active,
        TransitionOff,
        Hidden,
    }
}
